[tool.poetry]
name = "vaannotate"
version = "0.1.0"
description = "File-based multi-annotator NLP gold standard toolkit"
authors = ["AutoGenerated <noreply@example.com>"]
readme = "README.md"
packages = [{include = "vaannotate"}]

[tool.poetry.dependencies]
python = "^3.11"
typer = {version = "^0.9.0", extras = ["all"]}
rich = "^13.7.0"
python-dateutil = "^2.9.0"
PySide6 = "^6.7.0"
pandas = "^2.2.0"
pyarrow = "^15.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.1.0"

[build-system]
requires = ["poetry-core>=1.7.0"]
build-backend = "poetry.core.masonry.api"
